import java.util.Arrays;
import Window.*;
import Matrices.Matrix;

import javax.swing.*;

public class Main {

    public static void main(String[] args)
    {
        //Matrix matrixInstance = new Matrix();
        //double[][] test_matrix = {{2, 7, -1, 3, 2}, {0,0,1,0,1},{-2,0,7,0,2},{-3,-2,4,5,3},{1,0,0,0,1}};
        //double[][] test_metody_num = {{1,1,3},{3,2,4},{4,3,5}};
        //double[][] test_algorytmion = {{1, 2, 3, 4}, {2, 1, 0, -1}, {4, 3, -4, -11}, {7, 6, -4, -4}};
        //double[][][] lu = matrixInstance.create_LU_matrices(test_algorytmion);
        //matrixInstance.record_LU_to_file(lu);

        //for (int i=0; i<=lu.length; i++)
        //{
        //    System.out.println(Arrays.toString(lu[0][i]));
        //}

        //for (int i=0; i<=lu.length; i++)
        //{
        //    System.out.println(Arrays.toString(lu[1][i]));
        //}

        Frame frame = new Frame();
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setVisible(true);


       // System.out.println(calculate_det(test_matrix));
    }
}